1. oracle Setup 후 cmd 창을 띄워 

2. system 계정 접속 수 scott 계정 생성 및 권한 부여
C:\Users\Admin> sqlplus system/비밀번호
		   sqlplus "/as sysdba
SQL> create user scott identified by tiger
SQL> grant dba to scott;
SQL> exit;

3. scott 계정 접속 후 데이터 확인
C:\Users\Admin>sqlplus scott/tiger
SQL> SELECT * FROM TAB; -- 소유 테이블 확인

4. 수업에 사용할 데이터 넣기
SQL> @C:/ai_x/lecNote/04_DBMS/demo.sql;

5. scott 계정 접속 후 데이터 확인
C:\Users\Admin>sqlplus scott/tiger
SQL> SELECT * FROM TAB; -- 소유 테이블 확인
SQL> SELECT * FROM DEPT;
SQL> EXIT

6. SQL developer 를 다운로드 받아서 압축을 해제한 후 사용

